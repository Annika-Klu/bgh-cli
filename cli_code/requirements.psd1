@{
    RequiredModules = @(
        @{
            Name = "ImportExcel"
            MinimumVersion = "7.8.4"
            Description = "Erstellen von Excel-Dateien, ohne eine Installation von Excel auf dem PC zu benötigen"
        }
    )
}